# Codex 用実装プロンプト（v2.0.1: ツールバーのアイコン化）

以下をそのまま Codex に貼り付けて使う（/codex-cycle はヘッドレス実行する）。

---

macOS 画像注釈アプリ「Kakikomi」のツールバーを修正してください。仕様はリポジトリ直下 `DESIGN.md` の **§15「ツールバーのアイコン化 v2.0.1」** に確定済みです。まず §15 を熟読してください。既存の設計制約は一切変えないこと。UI レイアウトのみの修正で、ViewModel・レンダラ・モデルには手を入れません。

## 問題

`Kakikomi/Views/ContentView.swift` のツールバーで、各ボタンが `Label("開く", systemImage: "folder")` のようにテキスト+アイコンを描画し、さらにツールピッカーが8セグメント全てテキスト付きのため、ツールバーの必要幅がウィンドウ幅を超え、**右端の「ピクチャに保存」ボタンが見切れる**。

## 修正: アイコンのみ表示にする

- ツールバーの全ボタン（開く / ペースト / 画面取込 / ピクチャに保存）とツールピッカーの全8セグメントを、**SF Symbols のアイコンのみ**の表示にする
- **絵文字（Unicode 文字）は使わないこと**。常にカラー描画のためボタンの無効状態やテーマに追従せず、macOS のバージョンで字形が変わるため。既存の `systemImage` の SF Symbol をそのまま使う
- 各コントロールで以下を必ず維持・追加すること（アイコン化で情報が失われないための条件）:
  - `.help("…")` によるツールチップ（従来テキストだった文言をそのまま使う。例: 開く / ペースト / 画面取込 / ピクチャに保存、および各ツール名）
  - `.accessibilityLabel("…")` （VoiceOver 用。既存の `accessibilityIdentifier` はテスト用の別物なので、両方必要）
  - 既存の `accessibilityIdentifier` は**変更しない**（将来の UI テスト用の識別子のため）
- セグメンテッドピッカーの各項目は `Label(...)` を `Image(systemName: ...)` に変え、`.accessibilityLabel` を付ける（`.help` はセグメント単位では効かない場合があるが、付けておいて構わない）
- 簡易・詳細どちらのモードでもアイコンのみ（モードによる差はサイドインスペクタの有無だけ）
- 再発防止: 右端の操作ボタン群（ピクチャに保存・モード切替）に `.fixedSize()` を付けて圧縮・見切れを防ぎ、ツールピッカー側が先に圧縮されるようにする

## 受け入れ条件

- 最小ウィンドウ幅（900pt）で、ツールバーの全コントロール（左端の「開く」から右端のモード切替まで）が見切れずに収まる
- 各アイコンにマウスを載せるとツールチップで名称が分かる
- アイコンのみになっても、無効状態（画像未読込時）の淡色表示が従来どおり効く

## 検証

以下がすべて通ること（UI レイアウトの修正なので新規テストは不要。既存テストの非退行を確認する）:

```
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project Kakikomi.xcodeproj -scheme Kakikomi -configuration Debug build
```

既存テスト2本（コンパイルコマンドは `codex-fix-prompt.md` の検証節と同じ + `Kakikomi/Services/DragExportService.swift` をリンク）:
- `Tests/main.swift`（レンダラ）
- `Tests/ViewModelUndo/main.swift`（Undo）

## やらないこと

- ツールバーの構成変更（ボタンの追加・削除・並べ替え、オーバーフローメニュー化など）
- ViewModel・レンダラ・モデル・CanvasView の変更
- 既存アーキテクチャの変更、外部ライブラリ、指示外リファクタリング
