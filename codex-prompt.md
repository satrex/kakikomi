# Codex 用実装プロンプト

以下をそのまま Codex に貼り付けて使う。

---

macOS ネイティブの画像注釈アプリ（Skitch 代替）を実装してください。

まずリポジトリ直下の `DESIGN.md` を熟読してください。これが唯一の仕様書です。以下はその要点と、実装時に絶対に守ってほしい制約です。DESIGN.md と矛盾を感じたら DESIGN.md を優先し、判断に迷ったら Skitch の挙動に寄せてください。

## 作るもの（MVP）

- 画像を開く: ファイル選択 / ウィンドウへのドラッグ&ドロップ / Cmd+V ペースト
- テキスト注釈: クリックで配置しその場編集、ダブルクリックで再編集、ドラッグで移動、四隅ハンドルのドラッグでフォントサイズを拡大縮小、Delete で削除
- テキストは縁取り付き（既定: 白文字・濃灰 #1A1A1A の縁・薄い影・ヒラギノ角ゴ W7）
- 矢印注釈: ドラッグで描画、端点ハンドルで編集
- Undo / Redo（Cmd+Z / Shift+Cmd+Z）
- 保存: ツールバーのボタン一発で ~/Pictures に PNG 保存（ファイル名例: `注釈 2026-08-03 15.04.30.png`、衝突時は連番）。Cmd+C で結果画像をクリップボードへ。Cmd+Shift+S で NSSavePanel による別名保存（PNG/JPEG）
- インスペクタ: 文字色・縁取り色・縁取り幅（フォントサイズ比）・フォントウェイト（W6以上のみ）・矢印色

## 技術制約（違反しないこと）

1. Swift / SwiftUI アプリシェル、macOS 14+、Xcode プロジェクト（`xcodegen` や SwiftPM app ではなく通常の .xcodeproj で可）。
2. **キャンバスは AppKit のカスタム NSView（NSViewRepresentable で埋め込み）+ Core Graphics 描画**。SwiftUI の Canvas や Text で注釈を描画しないこと（縁取り文字が描けないため）。
3. **縁取り文字は 2 パス描画**: 同じ文字列を「ストロークのみ（`.strokeWidth` に正値 = フォントサイズ比%、CGContext に lineJoin/lineCap = round を設定）」→「塗り」の順で重ね描きする。負の strokeWidth による 1 パス方式は禁止（塗りが縁に侵食され細字が痩せるため）。
4. **縁取り幅はフォントサイズ比（ratio）で保持**する。絶対値 pt で持たないこと（拡縮時に縁が細く見えて要件が破綻する）。
5. **注釈の座標・フォントサイズはすべて「元画像のピクセル座標系」で保持**する。ビューは画像→ビューのアフィン変換を 1 つだけ持ち、描画とヒットテストの入口で変換する。書き出しは元画像ピクセルサイズの CGContext にそのまま描く。
6. **画面表示と書き出しは同一の描画コード**（`AnnotationRenderer`）を通すこと。書き出し専用の描画パスを別に書かない。
7. テキストのハンドルドラッグは**枠のリサイズではなくフォントサイズの変更**。対角のハンドルをアンカーとして固定し、アンカー〜マウス距離の比でフォントサイズをスケール（8〜400 px にクランプ）。
8. その場編集は NSTextView（または NSTextField のフィールドエディタ）をキャンバス上にオーバーレイして行う。編集確定は Esc または外側クリック。空文字で確定したら注釈を破棄。
9. Undo はスナップショット方式: 操作確定（mouseUp / 編集確定 / 削除）ごとに annotations 配列全体を UndoManager に登録する。差分方式にしないこと。
10. App Sandbox 有効。entitlements に `com.apple.security.app-sandbox`、`com.apple.security.files.pictures.read-write`、`com.apple.security.files.user-selected.read-write` を設定し、Pictures へは保存パネルなしで直接書き込む。
11. 注釈モデルは Codable な値型 struct。色は sRGB RGBA を持つ `CodableColor` struct で保持（NSColor を直接エンコードしない）。

## ディレクトリ構成

DESIGN.md §8 の構成に従うこと（Models / ViewModels / Views / Services）。

## 進め方と検証

DESIGN.md §9 のマイルストーン M1→M5 の順に実装し、**各マイルストーンごとに `xcodebuild build` が通ることを確認**してから次へ進むこと。最後に以下を自己チェックする:

- [ ] Sandbox 有効のまま ~/Pictures に PNG が保存される
- [ ] 保存した PNG の注釈位置・サイズが画面表示と一致する（Retina でズレない）
- [ ] テキストを四隅ハンドルで 3 倍に拡大しても、縁取りの太さの比率が見た目上保たれている
- [ ] 白背景・黒背景・細かい模様のスクリーンショットの 3 パターンで既定スタイルの文字が判読できる
- [ ] Undo/Redo が「配置・移動・拡縮・編集・削除」の全操作で正しく巻き戻る
- [ ] 画像を開いていない状態で保存等のボタンが無効化されている

## やらないこと

- v2 機能（矩形・楕円・モザイク・トリミング・スクリーンショット取り込み）
- ネットワーク通信、アナリティクス、外部依存ライブラリの追加（標準フレームワークのみ）
- App Store 提出用メタデータ（コード署名設定は Automatic のままでよい）
